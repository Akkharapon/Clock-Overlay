# Clock-Overlay
Clock Overlay


library KeyBoard hotkey level OS ,  System Tray Icon (pystray) Toggle Click-through

pip install keyboard pystray pillow


export .exe

python -m PyInstaller --onefile --windowed --name "ClockOverlay" CLOCK_GUI.py
